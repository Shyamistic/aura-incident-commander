{
  "name": "aura-root-infrastructure",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w -p tsconfig.json",
    "cdk": "cdk",
    "install:all": "npm install && cd service-orchestrator && npm install"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk-lib": "^2.133.0",
    "constructs": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@langchain/openai": "^1.1.3",
    "dotenv": "^16.4.5"
  }
}
